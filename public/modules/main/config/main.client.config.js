'use strict';

// Main module config
angular.module('main').run(['Menus', 'Employees',
	function (Menus, Employees) {
		// Config logic
		// ...
		// console.log(Employees.current_user);
		//   Employees.getCurrentUser();
	}
]);